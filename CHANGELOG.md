# AInvest CLI Agent 更新日志

## [4.0.0] - 2026-06-06

### 新增功能

- **行情看板总览升级**：左侧行情看板从简单卡片升级为投研仪表台，包含美股三大指数看板（道琼斯/纳斯达克/标普500）、全市场涨跌分布柱状图、行业板块涨幅/跌幅 Top 5
- **港美股 Mock 数据扩充**：新增腾讯控股(0700.HK)、阿里巴巴(9988.HK)、英伟达(NVDA)的行情、档案和 30 日走势数据
- **自选股覆盖多市场**：默认自选股更新为 AAPL / TSLA / 0700.HK / 9988.HK / NVDA
- **个股走势图**：个股详情页下方新增 30 日收盘价面积图，随股票切换自动更新
- **Session 消息持久化**：ChatStore 重构为 `messagesBySession` 结构并接入 `zustand/persist`，重启应用后对话记录完整保留
- **Agent 回复 Markdown 渲染**：Agent 回复支持加粗、列表、代码块、引用、链接等 Markdown 语法，流式期间纯文本展示，完成后自动切换
- **DevTool 显式开关**：聊天面板顶部新增扳手图标按钮（仅开发环境），可显式打开/关闭 Agent 日志看板
- **三栏背景色差异化**：行情看板/聊天面板/Session 列表三栏使用不同背景色，增强空间层次感

### 优化

- **Prompt 统一收口**：所有 inline prompt 迁移到 `prompts.ts`，版本号 `PROMPT_VERSION = 'v4.0.0'`

### 技术规范

- TypeScript 编译零报错
- ESLint 零错误
- recharts 图表库引入（按需使用 LineChart/BarChart/AreaChart）
- react-markdown 引入（Agent 回复富文本渲染）

## [3.0.0] - 2026-06-06

### 新增功能

- **Thinking 块展示**：Agent 处理用户请求时，在回复上方展示可折叠的思考步骤（意图识别 → 工具调用 → LLM 生成），让用户清楚知道 Agent 在做什么
- **DevTool 日志面板**：开发环境下按 `Ctrl+Shift+L` 唤起右侧浮动面板，实时查看 Agent 运行日志，支持按类型过滤和导出 JSON
- **自动命名 Session**：新 Session 发送第一条用户消息后，自动根据消息前 10 个字符生成名称；用户手动重命名后不再自动更新
- **快捷工具按钮**：输入框上方新增 4 个快速填充按钮（查行情/搜新闻/看档案/做回测），点击后填充模板到输入框，占位符自动选中
- **推荐问话卡片**：空 Session 时展示 4 个问题卡片，引导用户发起首次对话
- **LLM 意图评测脚本**：新增 `run-intent-llm-eval.ts`，可调用真实 LLM API 评测意图识别准确率，支持 `--model` 参数

### 优化

- **Prompt 统一收口**：所有 inline prompt 迁移到 `src/renderer/services/agent/prompts.ts`，单一数据源，版本号 `PROMPT_VERSION = 'v3.0.0'`
- **Logger 增强**：新增敏感字段自动过滤（`apiKey`/`authorization`/`token` 替换为 `[REDACTED]`）和事件订阅机制（`subscribeLogs`）
- **提炼偏好算法改进**：支持 10+ 种风险偏好变体映射，Prompt 增加提取示例和"不要猜测"规则
- **UI 交互优化**：`alert()` 替换为非阻塞通知条，避免模态弹窗导致输入框焦点丢失

### 修复

- Thinking 块标题在 Agent 完成后仍显示"正在思考" → 改为根据步骤状态动态显示"思考完成"/"思考异常"
- 提炼偏好结果为空（风险偏好未识别、关注板块无、摘要无）→ 放宽校验逻辑，支持变体表达
- `alert()` 阻塞导致输入框无法输入 → 替换为组件内通知条

### 技术规范

- TypeScript 编译零报错
- ESLint 零错误
- 意图识别评测集通过率：94.3%（基线一致）
- E2E 评测集通过率：86.7%（基线一致）

## [2.0.0] - 2026-06-05

### 新增功能

- LLM Agent 引擎（意图识别 + 工具调用 + 回复生成）
- 双层意图分类（本地正则 + LLM）
- 长期记忆系统（风险偏好、关注板块、摘要）
- 评测集和 prompt 审计机制

## [1.0.0] - 2026-06-04

### 新增功能

- 三栏布局（行情 40% / 聊天 40% / Session 20%）
- 基础聊天功能（流式回复）
- Session 管理（新建/切换/删除）
- Mock 工具层（行情/新闻/回测/档案）
